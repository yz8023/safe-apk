# v9.10.19

## 修复：字符串加密重写丢弃排除类导致的运行期崩溃

- **问题根因**：字符串加密器尊重排除规则时对 androidx/okio 等类 `continue` 跳过改写，但未将该类原样加入重写列表，DexPool 重写后这些类从 dex 中整体消失。任何「含排除类 + 命中敏感串」的混合 dex 开启字符串加密后，运行到相关类型引用（如 `okio.internal.-ByteString`）直接抛 ClassNotFoundException。
- **修复**：命中排除规则的类仅跳过字符串加密改写，原样加入 `newClasses` 保留，重写产物不再丢类。
- **验证**：构造「androidx 占位类 + 含敏感串业务类」混合 dex，加密前/后类清单一致（2/2 保留），敏感串仍被加密。
- 版本提升至 v9.10.19。
