# AndroidHardeningTool 版本发布说明

## v9.10.25

### 功能新增
- **混淆字典 v2（全新）**：完整移植 ArkProtector-v2.8.1 的 21 分组混淆字典生成能力
  - 21 个字符分组可选：阿拉伯组合辅音/埃塞俄比亚吉兹/韩文谚文/藏文堆叠/Zalgo 变音/泰文连体/零宽标记/蒙古文/格鲁吉亚/叙利亚/塔纳文/高棉文/加拿大音节/变体选择符/包围文字/麻将/多米诺/扑克/太玄经/埃及象形
  - 复杂度三档（低/中/高）：中高复杂度自动叠加变音符/层叠符生成高混淆度词条
  - 自定义 Unicode 区间（十六进制起止）与自定义符号字符集
  - 词条数与最短/最长长度可调，去重上限 50000
  - 支持【预览】（即时生成 20 条示例）、【导出】（写 prefs + 落盘 txt 到下载目录）、【导入】（OpenDocument 选择词表 txt，支持 # 与 // 注释行）
- **字典真正参与混淆**：导出/导入的词条接入类重命名与字段重命名命名池
  - 类名以 `L词条1/词条2;` 两段拼接（对齐 ArkProtector）
  - 字段名直接使用词条，词条池自动净化非法字符（`/;<>@` 等）
  - 加固启动时从持久化 prefs 恢复词条，无需重新导出
- **清单组件保护（加固正确性）**：class-rename 不再重命名清单引用的组件类
  - 新增基于二进制 AXML 解析的组件收集（application/activity/alias/service/receiver/provider/instrumentation 的 android:name），对随机/自定义包名入口类同样生效
  - SecurityCheckProvider 精确保护，防止自检失效
- **OOM 修复**：class rename 映射构建由每 dex 三次 `loadDexFile`（类型收集/字符串扫描/映射构建）合并为单次全量加载，峰值内存降至约 1/3

### 行为调整
- 首页「代码混淆」卡片内三折叠区（硬编码增强/自我保护/代码混淆）改为互斥展开，避免配置干扰

### 说明
- 字典 v2 覆盖旧 "< 默认/生成 >" 双模式对话框；保留 prefs 兼容键名（obfuscation_dict_count/dict_enabled_groups/dict_complexity/obfuscation_dict 等）

## v9.10.24

### 功能调整
- **代码混淆配置移入首页**：首页新增「代码混淆」可折叠卡片，集中管理字符串加密开关、最小加密长度、混淆字典与 9 项 DEX pass（类顺序打乱/头部混淆/Debug 移除/Goto/算术/控制流/调用间接化/方法重载/字段重命名/类重命名），无需进入设置页即可快速调整
- **混淆字典双模式**：支持「默认配置」（内置 120+ 词根组合生成拟真标识符，如 utilCacheManager）与「生成配置」（随机大小写字母+数字+下划线词条），自定义数量与最小词长后导出到下载目录

### 说明
- 设置页原代码混淆区块已移除，保留"仅抽取指定函数"与"剔除 ABI"配置