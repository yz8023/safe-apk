# v9.10.12

## 签名生成：可直接自定义起止日期

- 生成 keystore 时可直接填写 **生成日期** 与 **到期日期**（`yyyy-MM-dd`），证书有效期按你指定的日期真实编码。
- 结束日期默认为当天 23:59:59，保证到期日整天有效。
- 支持任意年份：
  - 2050 年以前日期走 UTCTime 短编码；
  - 2050 年及以后自动切换 GeneralizedTime（随便填到 9999 年都行）。
- 留空时回退为原快捷方式（10 年 / ~27 年 / 100 年）。

## 其他（v9.10.11 合并内容回顾）

- **修复 JKS 无法读取**：纯 Java JKS v2 解析 + KeyProtector 私钥解密，Android 上 jks/.keystore 可正常读取、查看与签名。
- **PK8/PEM 平台签名支持**：可直接选用 `platform.pk8` + `platform.x509.pem` 加固签名，私钥支持加密/未加密 PKCS8。
- **格式转换**：jks / pk8 / pem / p12 任意互转，转 P12 一键设为签名密钥，导出 PK8/PEM 用于外部签名。