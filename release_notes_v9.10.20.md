# v9.10.20

## 修复：短中文 UI 文案漏加密

- **问题根因**：字符串加密的全量兜底按字符数阈值（默认 minLen=6）判定，5 字及以下的中文文案（如"还没有任务"）不加密，明文留在 dex。
- **修复**：含 CJK（中文）的字符串按语义单元计长，2 字及以上即加密，不受 minLen 拦截；纯 ASCII 串仍按 minLen 阈值控制，避免英文短词过度加密。
- **验证**：minLen=6 下，5 字中文"还没有任务"加密触发（encryptedCount=1），类清单完整保留（2/2），明文消失。
- 版本提升至 v9.10.20。
