# v9.10.14

## 函数抽取：方案命中统计与显示优化

- **方案模板选取即统计命中数**：点击任意方案（内置或自定义）加载规则后，自动扫描当前 APK 并显示「将抽取 N / 总方法 (约 x%)」，让你在选方案时就能评估覆盖面。
- **规则编辑区实时命中预览**：输入规则变化自动统计命中方法数，附「重新统计」手动刷新；未选 APK 时明确提示。
- **方案列表显示优化**：
  - 每个方案显示名称 + 规则条数，当前正在编辑的方案高亮标记；
  - 主界面卡片不再整段展示规则，改为「条数」徽标 + 首条规则一行预览，界面更简洁明了。
- **命中统计与生产引擎一致**：统计逻辑与 FrostShell 实际抽取规则（精确/类通配/方法名关键词/regex:）完全同源，数字即真实将抽取的方法数。

## 其他（v9.10.13 回顾）

- 配置对话框双 Tab（规则编辑 / 方案模板），大输入区 + 实时分类统计 + 可折叠语法说明。