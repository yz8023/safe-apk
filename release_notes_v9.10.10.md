# v9.10.10 发布说明

## 新增：签名工具（查看 / 生成 keystore）

签名设置区新增「签名工具」入口，支持：

### 1. 查看签名信息

- 选择任意 keystore 文件（支持 p12 / pfx / jks / keystore / ks / bks，自动识别格式）
- 输入存储密码（及可选别名）后读取证书签名信息：
  - Subject / Issuer
  - 有效期（notBefore ~ notAfter）
  - 签名算法、密钥算法与大小
  - SHA1 / SHA256 / MD5 指纹
  - keystore 内全部别名列表

### 2. 生成 keystore

- **一键生成超强签名**：RSA 4096 + 100 年有效期 + 随机强密码（含大小写/数字/符号），生成后自动设为当前签名密钥
- **自定义生成**：自定义别名 / 密码 / 证书内容（CN、OU、O、L、ST、C）/ 密钥算法（RSA 2048/3072/4096 或 EC P-256/384/521）/ 有效期（10/27/100 年）
- 生成后自动设为当前签名密钥，并显示生成结果证书信息

### 附带修复

- 自签名证书有效期超过 2050 年时改用 GeneralizedTime 编码（此前 100 年有效期会被错误解析为 2026 年）

## 说明

v9.10.8 / v9.10.9 的修复均已包含（字符串加密写坏 dex 恢复兜底、大小变化 +0 修复、一键打开 APK）。
