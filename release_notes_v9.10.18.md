# v9.10.18

## 字符串混淆强化：杜绝脱壳后明文泄露

- **全量长度加密兜底**：字符串加密不再只认英文敏感词，长度达到阈值（默认 6）的中文提示语、业务文案等全部进密文池，脱壳后 dexdump 不再出现任何明文 const-string（实测"请输入有效邮箱"等全部消除，加密量 0 → 557 条/单 dex）。
- **短敏感词无条件加密**：token / apiKey / 登录 / 密码 / 验证码 / 会员 / 余额 等短串不受长度阈值限制，命中关键词即加密。
- **放开 try-block 方法跳过**：此前含异常处理（try/catch）的方法整体跳过加密，协程 continuation（invokeSuspend）内的中文文案全部漏网；现在可安全改写，加密后异常表、跳转偏移均经 dexlib2 重新计算，d8 字节码验证通过，运行时无 VerifyError 风险。
- **新增 42 个中文业务关键词**：请输入 / 登录 / 密码 / 邮箱 / 验证码 / 支付 / 余额 / 会员 / 授权 / 封禁 等。

## 关于页更新

- **版本号单点维护**：关于页显示版本改由 BuildConfig.VERSION_NAME 自动读取，不再与构建版本脱节。
- **Telegram 频道**：链接与名称更新为 @Forinxy。
- **新增 MT 论坛主页入口**：一键跳转。

## 回顾 v9.10.17

- native 检测轮询 1s → 4s，功耗降低 75%，检测覆盖面不变。