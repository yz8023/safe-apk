# v9.10.13

## 函数抽取界面优化

- **配置对话框双 Tab 化**：
  - 「规则编辑」：大输入区（200dp）+ 实时统计（条数/精确/通配/正则分类）+ 可折叠语法说明 + 方法浏览器入口。
  - 「方案模板」：关键词快捷生成、自定义方案保存、内置/自定义方案列表，与规则编辑分离不干扰。
- **主界面卡片升级**：启用后直接预览规则内容（前 4 行）与条数，无需进对话框即可核对当前配置。
- **交互优化**：方案加载/关键词生成后自动跳回规则编辑 Tab；所有入口打开对话框统一回到编辑 Tab。

## 其他（v9.10.11 / 9.10.12 回顾）

- **签名生成自定义起止日期**：直接填写生成/到期日期（yyyy-MM-dd），2050 年后自动 GeneralizedTime 编码。
- **JKS 修复**：纯 Java JKS v2 解析 + KeyProtector 解密，Android 上 jks/.keystore 可正常读取与签名。
- **PK8/PEM 支持**：`platform.pk8` + `platform.x509.pem` 可直接用于加固签名（加密/未加密 PKCS8 均支持）。
- **格式转换**：jks / pk8 / pem / p12 任意互转，转 P12 一键设为签名密钥。